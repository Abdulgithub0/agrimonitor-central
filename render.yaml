services:
  - type: web
    name: agrimonitor-central
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: WEATHER_API_KEY
        sync: false
